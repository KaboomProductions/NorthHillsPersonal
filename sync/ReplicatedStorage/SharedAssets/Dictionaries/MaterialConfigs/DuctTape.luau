--!strict

type MaterialConfig = {
	displayName: string,
	description: string,
	imageId: number?,
}

local config: MaterialConfig = {
	displayName = "Duct Tape",
	description = "Strong tape used for repairs and upgrades.",
}

return config
